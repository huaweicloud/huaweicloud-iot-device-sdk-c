generateOpenssl.sh是用来自动生成openssl库文件的，openssl 的lib文件路径：/usr/local/lib64，openssl 的include文件路径：/usr/local/include/openssl
generateZlib.sh是用来生成zlib库文件的，zlib的lib文件在当前目录
paho版本较多，需要自动关联openssl的路径，暂不提供自动编译脚本

paho/openssl/zlib均是在官网下载